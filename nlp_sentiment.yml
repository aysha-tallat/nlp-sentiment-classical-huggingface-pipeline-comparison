name: nlp-sentiment
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - scikit-learn
      - datasets
      - nltk
      - transformers
      - torch --index-url https://download.pytorch.org/whl/cpu
